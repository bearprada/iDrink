<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="lab.prada.android.app.idrink"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk
        android:minSdkVersion="8"
        android:targetSdkVersion="18" />

    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" />
    <uses-permission android:name="android.permission.BLUETOOTH" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.VIBRATE" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name="lab.prada.android.app.idrink.MainActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="lab.prada.android.app.idrink.SettingsActivity"
            android:label="@string/title_activity_settings" />
        <activity
            android:name="lab.prada.android.app.idrink.DeviceListActivity"
            android:configChanges="orientation|keyboardHidden"
            android:label="@string/select_device"
            android:theme="@android:style/Theme.Dialog" />
        <service android:name="lab.prada.android.app.idrink.service.BluetoothChatService" />

        <provider
            android:name="lab.prada.android.app.idrink.LogProvider"
            android:authorities="lab.prada.android.app.idrink"
            android:syncable="true" />

        <receiver android:name="lab.prada.android.app.idrink.AlarmBoardcastReceiver" >
            <intent-filter>
                <action android:name="com.manish.alarm.ACTION" />
            </intent-filter>
        </receiver>

        <activity
            android:name="lab.prada.android.app.idrink.ChartActivity"
            android:label="@string/title_activity_chart" >
        </activity>
    </application>

</manifest>
